# Ambiente
DJANGO_ENV=dev

# Segurança
DJANGO_SECRET_KEY=django-insecure-development-only-key-replace-in-production

# Database - SQLite para desenvolvimento
DATABASE_ENGINE=sqlite

# Hosts
ALLOWED_HOSTS=localhost,127.0.0.1

# Força uso do SQLite em dev (desativa PostgreSQL)
DB_PROFILE=dev